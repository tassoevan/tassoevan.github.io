# React + TypeScript + Vite

This template provides a minimal setup to get React working in Vite with HMR and Oxlint.

## Code quality

- `yarn lint` runs Oxlint.
- `yarn format` formats supported source files with Oxfmt.
- `yarn format:check` checks formatting without changing files.
